name: Pipeline-Test

on:
  push:

jobs:
  test:
    runs-on: self-hosted

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Test
        run: |
          echo "PIPELINE TRIGGERED"
          echo "Branch: $GITHUB_REF_NAME"