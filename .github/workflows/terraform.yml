name: Terraform CI-CD

on:
  push:
    branches:
      - feature/101
      - main

env:
  TF_VERSION: "1.9.8"
  WORKING_DIR: "./environment/dev"

  # Azure App Registration
  AZURE_CLIENT_ID: "7a8e96d0-1e46-4e44-8553-7598eef4f67e"
  AZURE_CLIENT_SECRET: "PR58Q~cL6NhdMfvNmgSm.qm8OX6qCQy~7cP47cy0"
  AZURE_TENANT_ID: "093e9319-5648-4618-8c7d-f585fb2ed3f9"
  AZURE_SUBSCRIPTION_ID: "2eeb0869-d474-4f94-a0b0-fb6a5d23d142"

permissions:
  contents: read

jobs:

  # =====================================================
  # FEATURE BRANCH
  # Init -> Fmt -> Validate -> Plan
  # =====================================================

  terraform-plan:

    name: Terraform Plan - Feature

    if: github.ref == 'refs/heads/feature/101'

    runs-on: ubuntu-latest

    steps:

      - name: Checkout Code
        uses: actions/checkout@v4

      # Azure Login - Service Principal + Client Secret
      - name: Azure Login
        uses: azure/login@v2
        with:
          auth-type: SERVICE_PRINCIPAL
          creds: |
            {
              "clientId": "${{ env.AZURE_CLIENT_ID }}",
              "clientSecret": "${{ env.AZURE_CLIENT_SECRET }}",
              "tenantId": "${{ env.AZURE_TENANT_ID }}",
              "subscriptionId": "${{ env.AZURE_SUBSCRIPTION_ID }}"
            }

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ env.TF_VERSION }}

      - name: Terraform Init
        working-directory: ${{ env.WORKING_DIR }}
        run: terraform init

      - name: Terraform Format
        working-directory: ${{ env.WORKING_DIR }}
        run: terraform fmt -check -recursive

      - name: Terraform Validate
        working-directory: ${{ env.WORKING_DIR }}
        run: terraform validate

      - name: Terraform Plan
        working-directory: ${{ env.WORKING_DIR }}
        run: terraform plan


  # =====================================================
  # MAIN BRANCH
  # Init -> Fmt -> Validate -> Plan -> Apply
  # =====================================================

  terraform-apply:

    name: Terraform Plan and Apply - Main

    if: github.ref == 'refs/heads/main'

    runs-on: ubuntu-latest

    steps:

      - name: Checkout Code
        uses: actions/checkout@v4

      # Azure Login - Service Principal + Client Secret
      - name: Azure Login
        uses: azure/login@v2
        with:
          auth-type: SERVICE_PRINCIPAL
          creds: |
            {
              "clientId": "${{ env.AZURE_CLIENT_ID }}",
              "clientSecret": "${{ env.AZURE_CLIENT_SECRET }}",
              "tenantId": "${{ env.AZURE_TENANT_ID }}",
              "subscriptionId": "${{ env.AZURE_SUBSCRIPTION_ID }}"
            }

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ env.TF_VERSION }}

      - name: Terraform Init
        working-directory: ${{ env.WORKING_DIR }}
        run: terraform init

      - name: Terraform Format
        working-directory: ${{ env.WORKING_DIR }}
        run: terraform fmt -check -recursive

      - name: Terraform Validate
        working-directory: ${{ env.WORKING_DIR }}
        run: terraform validate

      - name: Terraform Plan
        working-directory: ${{ env.WORKING_DIR }}
        run: terraform plan -out=tfplan

      - name: Terraform Apply
        working-directory: ${{ env.WORKING_DIR }}
        run: terraform apply -auto-approve tfplan